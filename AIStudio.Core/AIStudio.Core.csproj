<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Core záměrně bez závislostí. Pure POCO records (Models), interfaces (Interfaces),
       enums, malé helpery (Services). MVVM toolkit (ObservableObject, RelayCommand)
       i Avalonia patří do App vrstvy — Core musí zůstat UI-agnostic, aby šel
       teoreticky reusable v jiném front-endu (web admin, CLI, …). -->

</Project>
